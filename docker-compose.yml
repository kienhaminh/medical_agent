services:
  db:
    image: pgvector/pgvector:pg16
    container_name: ai-agent-db
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=medinexus
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: ai-agent-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  segmentation-mcp:
    build:
      context: .
      dockerfile: segmentation-mcp/Dockerfile.mcp
    container_name: medical-agent-segmentation-mcp
    ports:
      - "8010:8000"
    env_file:
      - .env
    environment:
      - MCP_TRANSPORT=streamable-http
      - HOST=0.0.0.0
      - PORT=8000
      # Override: inside the container, the FastAPI host is reachable via host.docker.internal
      - PUBLIC_UPLOAD_BASE_URL=http://host.docker.internal:8000/uploads
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
