"""Tool loader module.

Tools are now declared directly in the codebase (src/tools/)
and registered via ToolRegistry at import time.
This module is kept for backward compatibility but no longer loads from database.
"""
