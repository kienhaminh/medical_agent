"""adapters.py — Deprecated.

Tool conversion is now handled automatically inside ToolRegistry.register().
Plain functions passed to register() are wrapped with @tool at that point.

This module is kept as a stub for backward compatibility with any external
imports, but all logic has been removed.
"""
